import type { DashboardData } from "../../types";

/** PLACEHOLDER — replaced by the redesign fan-out. */
export function WindowControl(_props: { data: DashboardData }) {
  return <div className="nt-kicker" style={{ padding: 14, opacity: 0.6 }}>WindowControl — pending</div>;
}
