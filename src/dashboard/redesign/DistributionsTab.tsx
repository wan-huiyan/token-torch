import type { DashboardData } from "../../types";

/** PLACEHOLDER — replaced by the redesign fan-out. */
export function DistributionsTab(_props: { data: DashboardData }) {
  return <div className="nt-kicker" style={{ padding: 14, opacity: 0.6 }}>DistributionsTab — pending</div>;
}
