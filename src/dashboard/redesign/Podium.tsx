import type { DashboardData } from "../../types";

/** PLACEHOLDER — replaced by the redesign fan-out. */
export function Podium(_props: { data: DashboardData; onOpenSession: (id: string) => void }) {
  return <div className="nt-kicker" style={{ padding: 14, opacity: 0.6 }}>Podium — pending</div>;
}
